# Weights

`model.pt` - final submitted model (rung 7).

    2.76 M parameters, 11.1 MB, fp32 state_dict
    Restorer(dim=64) as defined in evaluate.py / src/model.py

Loaded automatically by evaluate.py; no download step required.

    torch.load('weights/model.pt', map_location='cpu')
