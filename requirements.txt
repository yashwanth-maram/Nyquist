# Dependencies for run.py (inference / grading path).
#
# torch is left as a lower bound rather than an exact pin: the correct build
# depends on the grader's CUDA version, and a hard pin is the most likely way
# to break installation on an unknown GPU box. Tested on torch 2.0-2.13.
torch>=2.0.0,<3.0.0
numpy>=1.24.0,<3.0.0

# Required by preview.py / make_figures.py only, not by run.py:
matplotlib>=3.5,<4.0.0

# Required by train.py only (see requirements-train.txt for the full training env):
# pytorch-msssim>=1.0.0
# scikit-learn>=1.3.0
